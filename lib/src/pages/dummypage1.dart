import 'package:flutter/material.dart';

class Dummy1 extends StatelessWidget {
  const Dummy1({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('테스트 위젯 1입니다.'),
      ),
    );
  }
}
