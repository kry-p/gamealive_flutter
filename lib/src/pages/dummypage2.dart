import 'package:flutter/material.dart';

class Dummy2 extends StatelessWidget {
  const Dummy2({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('테스트 위젯 2입니다.'),
      ),
    );
  }
}
